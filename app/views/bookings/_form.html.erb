<%= simple_form_for [@camping, @booking] do |f| %>

  <div class="row">
    <div class="col-md-6">
      <label>Check In</label>
      <%= f.text_field :start_date, placeholder: 'Start Date', class: 'form-control' %>
    </div>
    <div class="col-md-6">
      <label>Check Out</label>
      <%= f.text_field :end_date, placeholder: 'End Date', class: 'form-control'%>
    </div>
  </div>

  <%= f.hidden_field :camping_id, value: @camping.id %>
  <%= f.hidden_field :user_id, value: current_user.id %>



  <br/>
  <%= f.submit "Book Now", class: "btn btn-primary" %>
<% end %>

